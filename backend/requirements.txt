--extra-index-url https://download.pytorch.org/whl/cpu
setuptools>=70.0        # provides pkg_resources; required by statsmodels build on Python 3.12+
psutil>=5.9.0           # RAM monitoring — /api/debug/memory endpoint
fastapi==0.115.0
uvicorn[standard]==0.30.6
anthropic==0.40.0
yfinance>=1.2.1
pandas==2.2.3
numpy>=1.26.4,<3          # allow 2.x — needed by statsmodels 0.14.4+ / patsy 1.x
scipy>=1.14.1             # relax to allow compatible version with numpy 2.x
python-dotenv==1.0.1
pydantic==2.8.2
httpx==0.27.2
cachetools==5.5.0
slowapi==0.1.9
pandas-datareader==0.10.0
fredapi>=0.5.1              # FRED API client — uses FRED_API_KEY env var when set
# ── Forecast engine dependencies ──────────────────────────────────────────────
arch>=6.3.0            # GARCH(1,1) — Engle (1982), Bollerslev (1986)
hmmlearn>=0.3.2        # Hidden Markov Model — Hamilton (1989)
scikit-learn>=1.4.0    # Ledoit-Wolf covariance, MinMaxScaler for LSTM
statsmodels>=0.14.0    # VAR — Sims (1980), Campbell et al. (2003)
# tensorflow-cpu REMOVED — Attention-LSTM moved to browser (TF.js) in Phase 1
# Model served from frontend/public/models/lstm/ as static Vercel asset (~400KB)
# Saves ~450MB server RAM; Railway Hobby has 8 GB for TimesFM.
# ── TimesFM 2.5 — always-warm server-side foundation model ────────────────────
# CPU wheel from PyTorch index (~200 MB, avoids 2 GB CUDA build).
# --extra-index-url at top of this file points to download.pytorch.org/whl/cpu.
torch>=2.3.0
# Official google-research/timesfm package from GitHub master.
# Do NOT use `transformers`'s TimesFm2_5ModelForPrediction — that integration
# silently drops most checkpoint weights (verified 2026-05). The official
# package loads all 231M params correctly via TimesFM_2p5_200M_torch.
timesfm @ git+https://github.com/google-research/timesfm.git
